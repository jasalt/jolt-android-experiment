# Apple Silicon Linux GTK host prerequisite

Determine whether the current Apple Silicon macOS host can provide the native
Linux environment required for the GTK reference application. GTK is not a
macOS validation target; the project requires a native ARM64 Lima guest or a
separate Linux host.

See [actual.md](actual.md) for the bounded host-resource result.
