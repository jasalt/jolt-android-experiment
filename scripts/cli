#!/usr/bin/env bash
# Run from the pinned Nix development shell:
#   nix develop -c ./scripts/cli --event '{:type :counter/inc}'
set -euo pipefail

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_root"
exec jolt run -m poc.main "$@"
